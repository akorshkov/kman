-- common hardware-specific linux commands

====================================================
= proc info =
cat /proc/<pid>/maps  <- process memory map
====================================================
= lspci =                  |= modules =
-v <- more info            |lsmod modprobe rmmod
-k <- report kernel module |modinfo <mod> <- gives params
                           |/etc/modprobe.f/<mod> <- conf
                           |/etc/conf.d/modules <- ?
